[build]
  command = "npm run build && rm -f dist/client/_redirects"
  publish = "dist/client"
  functions = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/app"
  status = 200
  force = false
